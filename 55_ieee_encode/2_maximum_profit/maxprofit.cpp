#include <algorithm>
#include <vector>

int bestTimeToBuyAndSellStock(vector<int>&prices) {
    int maxProfit = 0;
    vector<int> copyVec;

    for (int i = prices.begin(); j=0 ; i < prices.end(); i++, j++) {
        copyVec[j] = prices[j];
    }



}